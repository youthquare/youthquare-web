import React, {Component} from 'react';
import Layout from '../common/Layout';

class Feed extends Component {
    render() {
        return (
            <Layout>
                Feed
            </Layout>
        );
    }
}

export default Feed;
